<view class="container">
  <view>
    <block wx:for="{{nodes}}">
      <view class="node-item" bindtap="fnOpenZxxx" data-id="{{item.id}}">
        <text class="node-title">{{item.title}}</text>
      </view>
    </block>
  </view>


  <loading hidden="{{hidden}}">加载中</loading>
</view>